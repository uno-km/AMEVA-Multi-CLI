import { test, expect } from '@playwright/test'

test('App starts and shows title', async () => {
  // Just a stub for E2E since we can't easily launch electron in this limited container environment without XVFB
  expect(true).toBeTruthy()
})
